interface:
  display_name: "Xcode Quality Gate"
  short_description: "Build, test, lint, and check deterministic project quality"
  default_prompt: "Run the appropriate repository quality gate, preserve real command results, and report any failure without weakening tests or settings."
policy:
  allow_implicit_invocation: true

# 小改动模板

即使是小改动也要限定范围，避免 Codex 顺手重构。

```text
目标：
当前行为：
期望行为：
允许修改文件：
禁止修改文件：
必须保持的回归：
验证命令：
```

要求：

- 先检查 git diff 和相关测试基线。
- 不改公共架构、依赖、动画或手势，除非目标明确要求。
- 添加最小测试。
- 只做必要差异。
- 运行指定验证和 `git diff --check`。
- 报告实际结果后停止。
